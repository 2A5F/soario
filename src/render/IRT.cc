// #include "IRT.h"

namespace ccc
{
    // D3D12_RESOURCE_STATES to_dx_state(const GpuRtState state)
    // {
    //     if (state == GpuRtState::Present) return D3D12_RESOURCE_STATE_PRESENT;
    //     else if (state == GpuRtState::RenderTarget) return D3D12_RESOURCE_STATE_RENDER_TARGET;
    //     std::unreachable();
    // }
} // ccc
